# --- Secrets: NEVER commit ---
.env
# Private signing keys (c2pa-rs es256 TEST key and any other). Public test CA
# certs and trust-settings JSON are safe to commit; private keys never are.
es256_private.key
*.key
certs/*.key

# --- PHP / Composer ---
/vendor/
composer.lock
.phpunit.cache/
.phpunit.result.cache
.phpstan.cache/
.deptrac.cache
.php-cs-fixer.cache

# --- Node (signing service) ---
node_modules/

# --- Spike / build artifacts ---
out/
tools/

# --- Editors / OS ---
.idea/
.DS_Store

# Local project instructions (not published)
CLAUDE.md
