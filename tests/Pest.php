<?php

declare(strict_types=1);

/*
 * Pest configuration.
 *
 * As specs are approved and tests are written, bind the shared base test case
 * and global expectations here, e.g.:
 *   uses(ContentCredentials\Tests\TestCase::class)->in('Feature');
 *
 * Intentionally minimal while the codebase is scaffold-only (no feature code
 * exists yet — see the spec-driven workflow in CLAUDE.md).
 */
