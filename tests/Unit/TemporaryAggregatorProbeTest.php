<?php

declare(strict_types=1);

/**
 * TEMPORARY — proves the all-green aggregator fails when an upstream job does.
 * Removed in the next commit on this branch.
 */
it('fails on purpose to verify branch protection would block this', function () {
    expect(true)->toBeFalse();
});
